[project]
name = "wiki-rag-api"
version = "0.1.0"
description = "API service for wiki-rag"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
  "boto3>=1.34",
  "pydantic>=2.6",
  "psycopg[binary]>=3.1",
  "pgvector>=0.2",
  "llama-index-core>=0.10",
  "llama-index-embeddings-bedrock>=0.1",
  "llama-index-vector-stores-postgres>=0.1",
  "llama-index-llms-bedrock-converse>=0.1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.coverage.run]
source = ["src/api"]

[tool.coverage.report]
fail_under = 78
show_missing = true
skip_empty = true
